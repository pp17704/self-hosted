apiVersion: v1
kind: Service
metadata:
  name: flask-app-gitops-service
spec:
  selector:
    app: flask-app-gitops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
